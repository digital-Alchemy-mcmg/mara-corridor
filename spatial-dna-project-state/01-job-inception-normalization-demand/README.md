# Job inception, normalization, demand

The supplied `spatial dna training.xlsx` is in `../originals/transferred/` and its worksheet data are in `../extracted/workbooks/spatial dna training/`. The separately committed workbook from `main` is in `../history/branch-snapshots/main/`. The two binaries differ. The reports and `SPATIAL DNA.docx` describe source wording, normalization, receptor identifiers and JOB-002; consult their original and extracted forms. No autonomous normalizer or receptor extractor was supplied as executable source.
