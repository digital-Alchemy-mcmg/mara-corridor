# Recovery from repository alone

1. Check the branch tip and compare all paths and SHA-256 values against `../manifests/files.json`.
2. Read `../provenance/transfer-index.md` and inspect originals under `../originals/transferred/`. Use the corresponding `../extracted/` text, CSV, and OCR for search and data access; consult the original for styling, graphics, and OCR errors.
3. Inspect `../history/branch-snapshots/` and the repository's own Git history. The snapshots include blank placeholders, the base workbook, the DNA structure document and the ENGine fragment. Do not infer a populated graph or full engine from their filenames.
4. Read `state-audit.md` and `../open-problems/discrepancies.md`. Compare any later execution directive and newly supplied artifacts against this baseline before implementation.

What remains unavailable from these transfers: the separately described Notebook collection and project ZIP, original populated 44-node/11-edge machine graph, complete running Stage 1–5 code, independent coordinate run, and executable canvas. If those exist elsewhere, their bytes must be supplied and verified separately. The conversation-specific audit has been encoded here; the precise conversational sequence itself is not a supplied source artifact and is not reproduced verbatim.
