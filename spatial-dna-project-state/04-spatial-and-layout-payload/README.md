# Spatial and layout payloads

`MARA_LAYOUT_PAYLOAD_v1.source-example.json` was parsed from paragraphs in the transferred `MARA_JOB_002_PROJECTION_PAYLOAD.json.docx`, not produced by a repository pipeline. Its source location is recorded in the file manifest. The full document-order transcription remains in `../extracted/documents/`. Preserve the example's values and provenance; it does not certify that the listed candidate claims have been checked against original evidence. A complete independently generated spatial payload artifact was not supplied.
