# Candidate evidence, Spatial DNA, traversal and binding

The reports and compiled DOCX describe six evidence planes, a reported 44 atoms, 11 edges, binding labels and five preserved conflicts. The `DNA` branch-tip snapshots are in `../history/branch-snapshots/DNA/`; `graph/nodes.jsonl`, `graph/edges.jsonl`, and `graph/schema.yaml` were zero-byte placeholders at that tip. Treat the named populated files and Binding Gate execution as reported claims pending the underlying source and run evidence.
